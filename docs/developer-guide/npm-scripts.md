# npm スクリプト

| コマンド | 内容 |
|---------|------|
| `npm run dev` | 開発サーバーを起動（http://localhost:8080、`vite/config.dev.mjs` を使用） |
| `npm run build` | 本番ビルド（`dist/` に出力、`vite/config.prod.mjs` を使用） |

- `dist/` ディレクトリはコミットしない
